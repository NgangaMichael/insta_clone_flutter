import 'package:flutter/material.dart';

class ToogleView extends StatelessWidget {
  const ToogleView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Text('Toogle View'),
    );
  }
}